import React from 'react';
import Header from '../components/Header'

const Home = () => {
    return (
        <div>
            <h1>React Shop</h1>
            <Header></Header>
        </div>
    );
};

export default Home;